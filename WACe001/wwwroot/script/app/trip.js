// /script/app/trip.js

// Exclude from global scope.
(
	function()
	{
		"use strict";

		// Create the module.
		// Last modification:
		// Add dependency on spinner directive.
		angular.module("app-trip", ["spinner"]);
	}
)();
